# Production env for pir2pir-docs-site — DO NOT COMMIT.
# Written to /opt/pir2pir-docs-site/.env by the deploy workflow from the ENV_PROD variable.
# The site is static, so there is nothing secret here; the file exists to keep the port configurable
# and to match the deploy shape of the other services.

DOCS_HTTP_PORT=8081
